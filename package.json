{
  "name": "hugo_site",
  "version": "1.0.0",
  "description": "This is the Hugo version of the repo for https://brycewray.com.",
  "main": "postcss.config.js",
  "scripts": {
    "start": "hugo server",
    "startIP": "hugo server --bind '192.168.254.10'",
    "build": "rm -rf public && hugo --gc --minify",
    "testbuild": "rm -rf public && hugo server --gc --minify",
    "testbuildIP": "rm -rf public && hugo server --gc --minify --bind '192.168.254.10'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^9.8.6",
    "cssnano": "^4.1.10",
    "npm-run-all": "^4.1.5",
    "postcss": "^7.0.35",
    "postcss-clean": "^1.1.0",
    "postcss-cli": "^7.1.2",
    "postcss-css-variables": "^0.14.0",
    "postcss-import": "^12.0.1",
    "postcss-nesting": "^7.0.1",
    "postcss-partial-import": "^4.1.0",
    "postcss-preset-env": "^6.7.0",
    "tailwindcss": "^1.8.10"
  },
  "engines": {
    "node": "10.x"
  },
  "devDependencies": {}
}
